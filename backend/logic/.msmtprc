# Set default values for all accounts
defaults
port             1025
tls              off
auth             off
logfile          ~/.msmtp.log

# Define the Mailpit account
account          mailpit
host             127.0.0.1
from             support@eventease.moe

# Set Mailpit as the default account
account default : mailpit

